经过两周的学习，对JS有了一定的了解，为了巩固，特别在此小结。
1.js本质是动态、弱类型、基于原型的语言。发明者是网景公司（NetScape）的布兰登·艾奇（Brendan Eich，1961年～）。
2.动态，弱类型，基于原型既是js的特点，也是js的难点。
3.动态：运行时确定类型，新手常犯一些错误，重构时也困难。
4.弱类型：没有固定的类型，一切皆对象。
5.原型：proto。这是js的难点，由此延伸出的原型，原型对象，原型链，闭包等等概念，和C#以及java的思维并不一样。
6._proto_和prototype的区别：前者是真正的原型链，指向构造此对象（函数）的prototype（原型对象。prototype就是原型对象，本质也是对象。
只有函数才有原型对象，原型对象有个默认的属性，指向当前对象的构造函数。因此所有的函数都有构造函数，换句话说，就是任何一个函数都可以作为构造函数来new一个实例。
js的并无类的概念，一切皆对象。js的继承通过原型链实现，_proto_指向构造此实例的构造函数的prototype，构造函数的_proto_指向前一个构造函数的prototype，以此类推，直到object的_proto_指向null。
具体关系可由下图揭示：
